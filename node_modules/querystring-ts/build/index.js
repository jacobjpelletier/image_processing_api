"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function stringify(query, seperator = "&", equal = "=") {
    return Object.keys(query)
        .map((key) => `${encodeURI(key)}${equal}${Array.isArray(query[key]) ? query[key].join(seperator) : query[key]}`)
        .join(seperator);
}
exports.stringify = stringify;
